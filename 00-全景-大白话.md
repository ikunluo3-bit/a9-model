# A9 物理全景（大白话版）

不堆术语，讲清楚这游戏的车到底是怎么跑起来的。
技术细节和地址在 `01-代码层/`，数值在 `02-字段表/` 和 `03-车辆档案/`。

---

## 一、最重要的一件事：面板数字是假的

游戏里有**两套完全不搭界的数字**，住在两个不同的文件里。

**第一套：车库面板看到的**（住在 `A9-business.gdb`）
极速 540、加速 98.2、操控 201、氮气 91.13、性能分 6048——
这些只管**显示、排名、赛事门槛**。它们**不控制车怎么跑**。

**第二套：真正开车用的**（住在 `CarPhysics.gdb`）
每台车 428 字节，107 个格子。车怎么加速、怎么抓地、氮气多猛，全在这。

两套数字之间**没有换算关系**。塞纳面板写 540、物理里写 432，
不是谁算错了，是**本来就是两次独立填的数**。你们当年也是用两个不同脚本改的。

> 原厂比例 358.4 ÷ 268 = 1.337，改完 540 ÷ 432 = 1.25。
> 面板和物理的原厂对应关系已经被打破了——不影响开，但面板数字更"不可信"了。

---

## 二、车在一帧里经历了什么

```
    路面是什么材质？（沥青/草地/减速带…共 32 种）
              ↓
    查这种材质的抓地表  →  乘上这台车自己的抓地系数  →  写进每个轮子
              ↓
    轮子接地吗？───否──→  这个轮子这一帧啥也不算
              │是
              ↓
    发动机扭矩 ÷ 轮子半径 = 驱动力  →  推车
              ↓
    悬挂：压缩量 × 弹簧 − 压缩速度 × 阻尼 = 支撑力
              ↓
    转向：查一张"速度→转向能力"的表（只覆盖到 500 km/h）
              ↓
    所有力加起来 → 车动了
```

**"接地"是总闸门。** 判定接地的那个函数在整个物理层被调用了 30 次——
抓地力、驱动力、接触处理，全都先问一句"这轮子挨着地没有"。
不接地，这个轮子这一帧就完全不参与。

---

## 三、各系统怎么工作

### 抓地力：三个东西相乘

```
最终抓地力 = 路面材质的抓地 × 这台车的抓地系数 × 一个全局修正
```

路面有 32 种（沥青新/旧、石头、泥、沙、砾石、草、树叶、金属、水、木头、
地毯、冰、雪、**搓板路**、水泥、**空气**、玻璃、塑料、花、熔岩……），
每种有自己的抓地值。**减速带就是里面的"搓板路"那一类。**
连"空气"都算一种材质——不接地的时候用它。

**车的抓地系数是独立乘上去的**，不是跟材质混在一起算的。
所以你把塞纳抓地拉到 2.5 倍，是在**所有路面上同时**变强。

### 抓地力一个变量，管三件事

这是你自己发现的，代码层面完全成立：

- **横着看**：转向力 → 转向变敏感、过弯半径变小
- **绕着车身滚转轴看**：侧向力把车往外压 → 载荷转移 → **抬轮**
- **顺着走**：轮胎能传递的推力上限

所以抬轮跟阻尼没关系——**稳态抬多高，等于「侧向力 × 重心高度 ÷ 轮距」，
这个式子里根本没有阻尼**。阻尼只决定抬起来多快、落下去多快。

### 硬拐掉速

不漂移硬拐，轮胎会侧滑，侧滑吃能量就掉速。
抓地力大 → 同样打方向盘，侧滑角更小 → 掉速更少。
你实测"改完硬拐惩罚明显变小"，就是这么来的。

### 氮气：四档，每档四个数

游戏给每台车配了四组氮气参数，每组四个数：

| | 怎么触发 | 消耗 | 加速 | **维持** |
|---|---|---|---|---|
| **单喷** | 点一下 | 最慢 | 最小 | 中等 |
| **橙喷** | **双击**（随时，氮气没空就行） | 快 | 大 | **全车最低** |
| **蓝喷（完美氮气）** | 氮气条蓝区里点一下 | 慢 | 中 | **全车最高** |
| **紫喷** | 氮气条满 + 规定范围内双击 | 最快 | 最大 | 几乎为零 |

**"维持"这一项是这次挖出来的**——它决定"按着氮气硬拐会不会掉速"。
蓝喷这个值全车 290 台**无一例外**是最高的，橙喷无一例外是最低的。
所以蓝喷硬拐不掉速、橙喷纯爆发不耐用，是设计好的，不是错觉。

橙喷消耗最快也有道理：**双击等于吃两份氮气**。

### 速度：两个上限，你们只改了一个

| | 值 | 改过吗 |
|---|---|---|
| 地面速度上限 | 你们设成 **432 km/h** | ✅ 改了 |
| **空中速度上限** | **600 km/h** | ❌ **一直是游戏原生值** |

**你看到的 600 就是这个空中上限。** 它从来没被动过——
当年那个脚本的注释白纸黑字写着"游戏另有一个 600 km/h 的空中上限，故意不碰"。

432 是"推力衰减到零的那个点"，600 是"硬顶"。
**中间这段没有任何东西主动把速度拽回去**——所以直线能到、触地漂移能到、
松开氮气也不掉。不是 bug，是两套上限之间的空档。

### 转向表只算到 500 km/h

游戏预先算好一张 30 个点的表，覆盖 10 到 500 km/h，
每个速度对应一个转向能力。**超过 500 km/h 就没有表了**，
参数停在表的最后一格不再变。

**你现在跑 600，已经在表外了。** 高速段手感有什么异常，多半是这个边界。

### 悬挂

每个轮子有：弹簧硬度、压缩阻尼、回弹阻尼、半径。
力的算法就是最标准的那个：`压缩量 × 弹簧 − 压缩速度 × 阻尼`。

### 驱动力

```
扭矩 ÷ 轮子半径 = 推力
```
但有个前提：**两个驱动轮都得接地**，只要一个离地，这一帧不给驱动力。

---

## 四、游戏怎么防你改内存

物理值在内存里**不是普通的小数**。每个值占 12 字节，**同一个数存三份**，
每一份都跟"它自己存放的地址"和"一个开机时随机生成的钥匙"异或过。
读的时候三份解出来必须**一模一样**，对不上就跳进反作弊。

所以想在运行时改一个物理值，你得：同时改对三份、每份算对各自的地址、
还得拿到当前这次开机的三把钥匙——少一样就炸。

**这就是为什么"改 gdb 资源文件"才是干净路子**，你们当年的选择是对的。

---

## 五、数据长什么样

**每台车 428 字节 = 107 个格子**，但不是 107 个小数——中间**混着整数**
（有 11 个是 8 字节整数，3 个是 4 字节整数）。按小数读那些格子会得到 0 或天文数字。

107 个格子里：
- **38 个已经知道是什么**
- **44 个全车一模一样**（引擎默认值，不造成车与车的差别）
- **25 个随车变化但还不知道是啥** ← 车的"个性"就藏在这

大部分格子是**成对**的：一个「没升级时的值」，一个「满级时的值」，
游戏按你的升级进度在两者之间插值。全表检出 **34 对**。

**记录长度还不一样**：普通车 428 字节，电动车 452/468 字节。
多出来的部分在尾巴上追加了几组 `(0, 1000)` 区间。
**注意：电动车的格子顺序跟普通车对不上，不能拿同一张表去改。**

---

## 六、你们那次改动，到底改了什么

| 改的东西 | 从 | 到 | 幅度 |
|---|---|---|---|
| 地面速度上限 | 268 km/h | 432 km/h | — |
| 加速度 | 2.414 | 3.380 | ×1.4 |
| **轮胎抓地** | **0.7848** | **1.9621** | **×2.5** |
| 氮气消耗 ×8 | — | ÷1.2 | 时长 ×1.2 |
| 压缩阻尼 | 0.3 | 0.6 | ×2 |
| 回弹阻尼 | 0.3 | 1.2 | ×4 |

**最关键的是抓地那一项。** 原厂全车最高的是 Devel Sixteen 的 **1.1531**，
你们把塞纳做到了 **1.9621**——**比游戏里存在过的最大值还高 70%**。

游戏从来没在这个区间被测试过。抬轮、转向敏感、硬拐不掉速，
全都是这一个数拉出来的连锁反应。

顺带一提：塞纳有两个格子（76/77 = 0.6/1.0）是**全车最差档**的水平
（快车普遍 0.09~0.12），跟它中游的极速完全不匹配。
如果那真是阻力类的东西，塞纳原厂手感应该偏钝——
你们把抓地拉到 2.5 倍，很可能顺手把这个短板盖过去了。

---

## 七、还不知道的

| 项 | 为什么卡住 |
|---|---|
| 轮子 17 个格子里另外 14 个是什么 | 只有 spring/阻尼/半径 有实机佐证 |
| 那 25 个"车的个性"格子 | 需要按车实测反推 |
| 32 种路面各自的抓地数值 | 表是运行时注入的，静态文件里没有 |
| 空中上限 600 存在哪 | 同样是运行时结构，静态二进制里查无此物 |
| 腾空但不算"飞行"的中间状态 | 物理层符号被剥光，没有可锚定的名字 |

**这几项的共同点：数据只在游戏跑起来的时候才存在**，
不在任何文件里。静态能榨的已经榨干了，再往下要么按车实测反推，
要么在真机上读一次内存。

---

## 八、顺带：3D 车模能提取

跟物理无关，但既然挖到了就记着——

manifest 的解密钥匙是一句伪装成报错的字符串 `"Error 3452: file not found"`。
解开后 32496 条目全是明文路径，**959 个车模**，而且**车模全都没压缩没加密**，
直接读文件就行。

模型格式是 `jmox`（Gameloft 自家的），里面的名字用 **XOR 0xAB** 混淆。
塞纳车模 1.85MB，解出 9325 个名字——骨骼、车身、玻璃、车灯、卡钳、
可拆改装件、碰撞脱落件、氮气喷口锚点，全都在。

剩下的只是把二进制网格布局解出来，是工作量，不是未知数。


---

# 整车替换：让一台车彻底变成另一台

**已经能做到了。** 一条命令，让 D 级科迈罗从里到外变成 S 级女武神——
外形、贴图、碰撞、动画、物理、几何全换，只有车名和面板数字还是科迈罗。

## 为什么能成

游戏的数据是一堆 `.gdb` 文件，每个都是同一种结构：

```
[文件头][一条条记录首尾相接][记录数][每条记录一个 32 字节的目录项: key / 位置 / 长度 / 类型]
```

**记录本身是纯数据，车的身份只写在目录项的 key 上。**
所以把 A 车的记录内容搬到 B 车的位置，B 还是 B（存档、图鉴、联机都不受影响），
但跑起来、看起来全是 A。

## 三层都得换，少一层就出事

- **只换 3D 模型** → 闪退。引擎拿着"科迈罗的配置"去女武神的网格上找材质参数，
  找不到就地自杀（这是引擎自己写死的断言，不是野指针）。
- **换了模型也换配置（CarDef），但配置里的 key 没补全** → 车从车库里消失。
  CarDef 里有三个指向别处的 key：底盘、物理、**引擎声**。
  漏掉引擎声那个，游戏就认不出这辆车了。
- **三个 key 全补回受体自己的** → 成了。

## 一句话总结

**外观跟着 CarDef 走，物理跟着 CarPhysics 走，身份跟着 key 走。**
换车 = 搬前两样、留最后一样。
